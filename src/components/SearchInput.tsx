import { Input } from "@material-ui/core"
import React, { useCallback } from "react"

const SearchInput = () => {
    const onRemoveText = useCallback((event) => {

    }, [])

    const onChangeValue = useCallback((event) => {

    }, [])


    return <Input />
}


export default SearchInput